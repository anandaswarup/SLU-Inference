[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "argparse>=1.4.0",
    "path>=17.1.1",
    "speechbrain>=1.0.3",
    "torch>=2.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.0",
]
